# LiveKit Voice Agent — 环境变量示例
# 用法: cp .env.example .env  然后填入你自己的 key
#
# 本 demo 用 OpenAI Realtime 模型(一个 key 同时做 STT+LLM+TTS),
# 所以只需要一个 OPENAI_API_KEY,不需要 LiveKit 服务器、不需要 Deepgram/Cartesia。

OPENAI_API_KEY=sk-your-openai-key-here
